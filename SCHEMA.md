# SCHEMA.md — DH_DataBase 知识库规则

> 状态：占位骨架，待填充完整规则。

## 三层结构
- 第一层 `raw/`：原始资料，只读，AI 绝不修改原件。
- 注：`raw/MANIFEST.xlsx` 是 AI 维护的**索引（非原始资料）**，可写；源文件本体严格只读。
- 第二层 `wiki/`：AI 编译维护的知识层（Markdown + `[[链接]]`）。
- 第三层 `build/`：部署产物（RAG 索引 / 微调参数 / 配置）。

## 页面类型
- entity（实体页）：产品 / 部件 / 设备 / 品牌
- concept（概念页）：技术 / 方法论
- topic（案例 / 综述页）：故障案例、手册综述
- manual（手册编译页）：每本手册一页

## 命名约定
- raw 资料：主体名保留不动；**无内部版本号**的文件在扩展名前追加 `_YYYYMMDD`（取文件最后修改日期）；已有版本号（v / V / Ver / _数字 等）保持原名；清理文件名末尾孤立的 `-`。
- 归类不依赖文件名：在 `raw/MANIFEST.xlsx` 用「适配产品线」列（多选，分号分隔）标注每条资料的归属。
- wiki 页：`<类型>-<标题>.md`（kebab-case）
- handover：`YYYYMMDD-主题.md`
- scripts：`<动词>_<名词>.py`
- tools：`tool-<name>/`

## 产品线定义（四大产品线 + 通用）
归类以 `MANIFEST.csv` 的「适配产品线」为准，命名规则见上。
- **音圈电机**：SAC-N1 / N2 / NP2 / NP4、SAC2-N1 等音圈电机本体。
- **电缸**：含三种形态——
  - 外驱配伺服驱动器（SAC-N2、SAC2-N1 等，原生 ECAT / CIA402）
  - 外驱配 485 驱动器（SAC-S、SAC-N）
  - 内置驱动器
- **电爪**：含两种形态——内置驱动器、外置 485 驱动器。
- **驱动器（产品线，限定定义）**：原生 ECAT 通讯、符合标准 CIA402 轴控协议的伺服驱动器（如 SAC-N2 / SAC2-N1 伺服驱动器、SAC-NP2 双轴驱动器、SAC 系列 ECAT 驱动器）。
  - ⚠️ **外置 485 驱动器（SAC-S / SAC-N）不单列驱动器产品线**：它仅是电缸/电爪内部控制板外置，用法与内驱一致，附属于对应电缸/电爪。
- **ECAT 盒子**：485→ECAT 网关，正式用途是适配电缸（外置 485 驱动器型）与电爪（外置 485 驱动器型）；适配产品线标 `电缸;电爪`。
- **通用**：力控、力显示器（第三方）、现场调研、通用操作 SOP 等跨产品线或不属四大核心的资料。

## 投喂协议
- 新增 / 更新 / 删减，统一格式：`投喂：<动作> <文件名>`
- 每次维护 `raw/MANIFEST.xlsx`，AI 据此增量编译。
- **索引即元数据，非原始资料**：`raw/MANIFEST.xlsx` 由 AI 维护（记录文件名/类型/版本/状态/适配产品线/哈希指纹），不计入「原始资料只读」约束；`raw/` 下的源文件（手册/视频/SOP 等）仍严格只读。

## 源文件新鲜度巡检（哈希指纹）
- 仅靠「文件名 + 版本」抓不到「文件名没变、内容被改」。给每个源文件算 **SHA-256 内容指纹**（+ `mtime` 修改时间 + `size` 字节数），存于 MANIFEST.xlsx 的 `sha256` / `mtime` / `size` 三列。
- 工具：`scripts/check_freshness.py`（依赖 openpyxl）
  - `baseline`：全量重新扫描 `raw/`，刷新指纹写回 `MANIFEST.xlsx`（同时登记新增 / 标记缺失文件）。
  - `check`：扫描当前状态与指纹比对，输出「🔴内容变更 / 🟡新增 / ⚪缺失 / 🟢未变」报告；存在变更/新增/缺失时退出码为 1，便于脚本化。
  - `maybe`：读取 `scripts/.freshness_state.json` 的 `last_check` 与 `interval_days`（默认 7 天）；超期则自动跑 `check`，否则提示未超期。
- **超时自动巡检约定**：用户每次来问文件 / 投喂相关事项时，AI 先跑 `maybe`；若距上次检查 ≥ `interval_days` 就自动执行 `check` 并把变更汇总告知，避免用户改了文件却忘了说。
- 不修改任何源文件：脚本只读 `raw/` 计算哈希，仅在 `MANIFEST.xlsx` 与 state 文件写元数据。

## 编译协议（ingest）

### 图片处理（轻量索引法）
> Fly 售后工作流：从原文件截图发给客户。wiki 起索引作用——搜到 wiki 页即知"去哪个文件哪页截图"。

- **不单独提取界面截图到图片文件夹**。理由：① 工作量大 ② 源文件改图后提取图需同步重提 ③ 用户用 wiki 时只看文字，图是给客户看的。
- wiki 手册页用统一标注格式索引界面截图：
  ```
  > 📷 界面截图：见原文件 P.XX — <面板/界面名>（用于<操作目的>）
  ```
- **例外**：跨多本手册复用的高频界面（如增益配置面板在调试 SOP、参数导入导出 SOP 都出现），可单独提图到 `wiki/assets/界面-<产品线>-<面板名>.png` 并在 wiki 引用。需 Fly 确认哪些算高频。
