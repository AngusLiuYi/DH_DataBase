## 2026-08-20 工作日志

## 型号后缀 BV 纠正（C040）：接口码 B/BV = 底部接口不带/带气路
- Fly 纠正：BV 是"带不带气路"（底部接口带气路，真空取放/吸嘴），不是抱闸
- 之前我猜 BV=抱闸，实际音圈选型手册第四章命名规则表 P14 明确：接口码 B=底部接口不带气路、BV=底部接口带气路
- 带气路版（BV）气压 -0.1~+0.2MPa、推荐输入负压 ≥-90kPa、流量 ≥6~8L/min
- 教训：型号后缀一律先查选型手册命名规则表，不凭猜测
- 沉淀：corrections C040、entity-音圈电机 售后速查补接口码、案例-005 型号注释补 BV、index 编号更新到 C040

## 华东工单 → FAQ 提取（服务工单(14).xlsx → raw/fae-qa/华东工单提取FAQ.xlsx）
- 源：D:\DownLoads\服务工单 (14).xlsx（sheet「服务工单」，53列，5075行）。目标模板 16 列 + 追加「筛选备注(理由)」列，第16列「参考价值」填 高价值/中价值。
- 范围确认：Fly 选「华东各组都算 + 全部时间」→ 取 服务人员组别 含「华东」的 5072 条，排除 3 条华南B组。
- 三档规则（规则化、非逐条人工，需抽检）：
  - 高价值：闭环(客户问题解决状态=关闭) 且 现象/排查步骤/结论 清晰。咨询类(简易咨询/客户应用使用/其他咨询类/选型等)按 Q&A 处理——现象=问题、结论=答案，两者清晰即高价值，步骤 NA 正常不降档；故障类需三栏都实打实有内容(步骤≥15字且含排查动作/编号，结论≥8字且含处理信号)。
  - 中价值：未闭环(跟进)有内容 / 闭环但某栏偏弱 / 咨询类问答不完整 / 培训SOP类。
  - 舍弃：陪产/现场配合类无技术内容(含「陪产」或固定套话)、现象与结论均无意义。未拷贝。
- 结果：高价值 3806、中价值 1099、舍弃 12；并去重剔除完全重复(现象+步骤+结论相同)155 条。落盘后重开校验无异常值。
- 待 Fly 拍板：高价值里咨询类 Q&A 占大头(约2600+)，若「高价值经验」专指带排查过程的故障案例，可把咨询类单拆或降档。
- 工具：openpyxl（venv 在 binaries/python/envs/default），pandas 因网络超时未装，只读用 openpyxl 足够。

## 华东工单 LLM 语义重筛（deepseek-v4-flash，全量 4905 条）
- Fly 要求「不靠字数、按语义逐条甄别、更严厉」。改用 DeepSeek API 逐条判（废弃本地 Ollama 方案）。
- key：用户写入 `D:\AI\DH_DataBase\build\configs\deepseek.env`（`DEEPSEEK_API_KEY=sk-...`）。模型名按 Fly 指定用 `deepseek-v4-flash`（非官方通用名，验证可用）。脚本 `scripts/classify_deepseek.py`（标准库 urllib 直调，零依赖，支持 --limit/--no-write/--resume）。
- 结果：全量 4905 条已重判。其中 4867 条 LLM 成功 → 高 3400 / 中 502 / 舍 965；另 38 条因 **HTTP 402 Payment Required（DeepSeek key 额度耗尽）** 失败，沿用旧规则值（高35/中3），备注标「LLM未重判(额度402回退旧值)」。
- 对比规则版（高3806/中1099/舍12）：LLM 严化后舍弃从 12 猛增到 965，符合 Fly 要的「更严厉」——主要砍掉演示测试、纯发手册/资料、重复描述、陪产等「长但水」工单。
- 坑1：openpyxl `save()` 写原文件报 **PermissionError**（文件被预览/Excel 占用）→ 改为写入副本 `raw/fae-qa/华东工单提取FAQ_annotated.xlsx`（数据一致 + 38 行备注改清晰），原文件 `华东工单提取FAQ.xlsx` 未动（仍是旧备注 LLM-ERROR）。备份在 `.bak_20260820`。
- 坑2：HTML 报告脚本用 `read_only=True` 加载大表 + 随机列访问会极慢/卡死；改用普通 `load_workbook()` 秒级完成（脚本 `scripts/gen_report_light.py`）。
- 交付：`华东工单提取FAQ_annotated.xlsx`（修正版，建议用这个）、`build/classify_full_report_final.html`（分档抽样报告）、`build/classify_deepseek_result.json`（4900条原始结果）。
- **收尾（Fly 决策）**：38 条不补跑 LLM，按现值（高35/中3）标注「LLM未重判(待人工)」留人工审核即可。已把修正版正式写回原文件 `华东工单提取FAQ.xlsx`（`save` 这次成功——原占用已释放），校验通过：4905 行、高3435/中505/舍965、备注 4867 条`LLM:已重判` + 38 条`LLM未重判(待人工)`。副本 `annotated.xlsx` 与 `.bak_20260820` 仍保留备用。


## 知识库语料「投喂」备料（华东工单FAQ → markdown 语料）
- Fly 投喂新文件 raw/fae-qa/华东工单提取仅技术服务&高价值FAQ_20260820.xlsx（2772 条，是 LLM 重筛结果的子集：仅 解决方式=技术服务 且 参考价值=高价值）。校验确认 2772 条全部满足这两个条件，无杂质。
- 动作：Fly 跳过「投喂意图」追问，按最合理默认——转成知识库可直接导入的 markdown Q&A 语料（每条含 现象/排查步骤/结论 + 元数据：问题类型/产品线/型号/客户/单号/部件）。脚本 scripts/gen_corpus.py。
- 产物：raw/fae-qa/华东工单FAQ_技术服务高价值_语料_20260820.md（2.34MB，2772 条）。
- 待办：FastGPT/Dify/RAGFlow 三 connector 当前均 disconnected，无法直接 API 投喂。需 Fly 连接系统后以此 md 为导入物料；或指定换格式(csv/拆分每篇一文件)/直连投喂。原 xlsx 未改动。
- 经验：PowerShell stdout 在此 sandbox 常被吞，查进程/输出一律写文件再 Read；杀后台卡死任务用 Stop-Process -Id <pid> -Force（无 TaskStop 工具）。

- 知识库投喂准备：Fly 要求把 2772 条高价值技术服务 FAQ 投喂到知识库。当前无连接配置/全 disconnected。按 FastGPT 默认写好 scripts/feed_fastgpt.py（直接读 xlsx 源、建数据集、逐条 insert text，含连通验证/并发4/失败重试/断点续跑）。待 Fly 提供 KbBaseUrl+KbApiKey（写 build/configs/kb.env）后运行；若实际是 Dify/RAGFlow 则换脚本。源: 华东工单提取仅技术服务&高价值FAQ_20260820.xlsx。

- 内驱电缸恢复出厂后初始化堵转（根因=零电角丢失）：客户恢复出厂设置会把零电角（电角度零点）清回默认，无刷电机换相角错→上电初始化即堵转。处理：重校零电角（寄存器 0x11/0x00=校准零电角，源 manual-dh-485寄存器表总览.md）+ 保存（0x0300 写1）+ 重启/重新初始化。注意校准前须空载无阻挡、否则校不准；若校准后仍堵转/待机电流>1A，按 faq-电缸 30275 增大寻相电流再校。佐证：topic-华东工单-电缸 多条（FWGD26071800014 撞料丢零电角、FWGD26073100005 回原中、FWGD26060600043 初始化过热、FWGD26030600022 待机电流大）、faq-电缸 1428/30275、电缸报警代码解析 C8 过电流含「未校零电角」。